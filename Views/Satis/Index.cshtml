@model MvcProje.Models.Entity.TBLSATISLAR
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>Satışlar Sayfası</h2>
<br />
@*<table class="table table-bordered">
    <tr>
        <th>Satış ID</th>
        <th>Ürün Adı</th>
        <th>Müşteri Adı Soyadı</th>
        <th>Adet</th>
        <th>Fiyat</th>
        <th>Sil</th>
    </tr>
    <tbody>
        @foreach (var satis in Model)
        {

        <tr>
            <td>@satis.SATISID</td>
            <td>@satis.URUN</td>
            <td>@satis.MUSTERI</td>
            <td>@satis.ADET</td>
            <td>@satis.FIYAT</td>
            <td>@Html.ActionLink("Satış Sil", "Sil", new { id = satis.SATISID }, new { @class = "btn btn-danger", onclick = "return confirm('Emin misin?')" })</td>

        </tr>

        }
    </tbody>
</table>*@

<button class="btn btn-primary" data-toggle="modal" data-target="#Modal1">
    Satış Yap
</button>
<div class="modal" id="Modal1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Satış Yap</h2>
            </div>
            <div class="modal-body">
                <form action="/Satis/YeniSatis" method="post">
                    <label>Ürün Adı: </label>
                    @Html.TextBoxFor(m => m.URUN, new { @class = "form-control" })
                    <br />
                    <label>Müşteri: </label>
                    @Html.TextBoxFor(m => m.MUSTERI, new { @class = "form-control" })
                    <br />
                    <label>Adet: </label>
                    @Html.TextBoxFor(m => m.ADET, new { @class = "form-control" })
                    <br />
                    <label>Fiyat: </label>
                    @Html.TextBoxFor(m => m.FIYAT, new { @class = "form-control" })
                    <br />
                    <button class="btn btn-success">Satış Ekle</button>
                    <button class="btn btn-default" data-dismiss="modal">Geri</button>
                </form>
            </div>
        </div>
    </div>
</div>